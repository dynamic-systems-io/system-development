# Example namespace-scoped SecretStore for development-system.
#
# Preferred path: use Crossplane provider claims in this folder:
# - 00-openbao-secret-store-provider.yaml (applied by default)
# - aws-secret-store-provider.example.yaml
# - azure-secret-store-provider.example.yaml
#
# This file is a manual fallback if you are not using Crossplane provisioning.
# Copy to secret-store.yaml, fill provider details, then apply:
# kubectl apply -f security/secret-store.yaml
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: secret-store
  namespace: development-system
spec:
  provider:
    # Choose ONE provider block below and remove the others.

    # Example: HashiCorp Vault
    vault:
      server: https://vault.example.com
      path: kv
      version: v2
      auth:
        kubernetes:
          mountPath: kubernetes
          role: development-system
          serviceAccountRef:
            name: default

    # Example: AWS Secrets Manager
    # aws:
    #   service: SecretsManager
    #   region: us-east-1
    #   auth:
    #     jwt:
    #       serviceAccountRef:
    #         name: default

    # Example: 1Password Connect
    # onepassword:
    #   connectHost: https://onepassword-connect.example.com
    #   vaults:
    #     platform: 1
    #   auth:
    #     secretRef:
    #       connectTokenSecretRef:
    #         name: op-connect-token
    #         key: token
